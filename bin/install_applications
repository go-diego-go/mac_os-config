#! /usr/bin/env bash

# Installs applications.

install_dmg_app "$DOCKER_APP_URL" "$DOCKER_VOLUME_NAME" "$DOCKER_APP_NAME"
install_dmg_app "$MIMESTREAM_APP_URL" "$MIMESTREAM_VOLUME_NAME" "$MIMESTREAM_APP_NAME"
install_dmg_app "$WALLY_APP_URL" "$WALLY_VOLUME_NAME" "$WALLY_APP_NAME"
install_dmg_app "$AMPHETAMINE_ENHANCER_URL" "$AMPHETAMINE_ENHANCER_VOLUME_NAME" "$AMPHETAMINE_ENHANCER_APP_NAME"
